# lunchbot

Fetch and post the menu to slack, also hosts a small website with only the menu.

Made for IMPACT A/S, so configuration is based on their setup.

For it to post to slack, make sure that `SLACK_WEBHOOK` is defined.
